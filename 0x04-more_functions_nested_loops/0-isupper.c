#include "main.h"

/**
 * _isupper - checks for uppercase letters
 *
 * @c: character to be checked
 *
 * Return: 1 if character is uppercase 0 if else
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
